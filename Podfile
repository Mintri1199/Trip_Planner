# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Trip_Planner' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Trip_Planner
  pod 'SwiftLint'
  target 'Trip_PlannerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Trip_PlannerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
